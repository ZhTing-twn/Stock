import { BarChart3, Calculator, Home, LineChart, Lightbulb, Target, Wallet, Settings, Search, Building2, ClipboardList } from 'lucide-react';
const nav=[['dashboard','首頁儀表板',Home],['quote','股票查詢',Search],['overview','股票總覽分析',ClipboardList],['fundamentals','基本面分析',Building2],['dca','定期定額',Calculator],['goal','目標反推',Target],['backtest','回測',LineChart],['indicators','技術指標',BarChart3],['cycle','景氣信號燈',Lightbulb],['portfolio','投資組合',Wallet],['fees','匯率與費用',Settings]] as const;
export function Layout({page,setPage,children}:{page:string,setPage:(p:string)=>void,children:React.ReactNode}){return <div className="min-h-screen md:flex"><aside className="md:w-72 bg-slate-950 text-white p-4 md:min-h-screen"><h1 className="text-xl font-bold mb-6">Stock Investment Analyzer</h1><nav className="grid gap-2">{nav.map(([id,label,Icon])=><button key={id} onClick={()=>setPage(id)} className={`flex items-center gap-3 rounded-xl px-3 py-2 text-left ${page===id?'bg-blue-600':'hover:bg-slate-800'}`}><Icon size={18}/>{label}</button>)}</nav><p className="mt-6 text-xs text-slate-400">僅供試算、回測與風險呈現，不提供個別操作建議。</p></aside><main className="flex-1 p-4 md:p-8 overflow-x-hidden">{children}</main></div>}
